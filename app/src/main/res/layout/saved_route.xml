<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ProgressBar
        android:id="@+id/progressBarSaved"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:indeterminateTint="@color/blue"
        android:layout_marginTop="350dp"
        android:layout_centerInParent="true"/>

    <ImageView
        android:id="@+id/imgSavedRoutes"
        android:layout_width="181dp"
        android:layout_height="120dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@drawable/saved_maps" />

    <EditText android:id="@+id/itemSearchSavedRoutes"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="12dp"
        android:layout_marginRight="12dp"
        android:layout_marginTop="150dp"
        android:hint="Search Saved Waypoints"
        android:inputType="text"/>

    <ListView
        android:id="@+id/editListViewSavedRoutes"
        android:layout_width="wrap_content"
        android:layout_height="516dp"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="200dp"
        android:layout_marginRight="12dp">

    </ListView>

</RelativeLayout>